import type { Metadata } from "next";

export const metadata: Metadata = { title: "协议与政策 · Wanke" };

export default function LegalLayout({ children }: { children: React.ReactNode }) {
  return children;
}
